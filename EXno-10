from sklearn.mixture import GaussianMixture
import numpy as np
X = np.array([[1], [2], [3], [8], [9], [10]])
model = GaussianMixture(n_components=2)
model.fit(X)
print("Clusters:", model.predict(X))
print("Means:", model.means_)
